return {
  'numToStr/Comment.nvim',
  opts = {
    mappings = {
      basic = true,   -- keep gcc / gc for normal comments
      extra = false,  -- disables gb, gcb, gco etc. to avoid conflicts
    },
  },
}

